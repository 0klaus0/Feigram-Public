# Feigram Website

这个目录是 Feigram 官网的静态页面，可以直接部署到 Cloudflare Pages。

## Cloudflare Pages

1. 新建 Pages 项目并连接 `g-star1024/Feigram-Public`。
2. Build command 留空。
3. Build output directory 填 `website`。
4. 绑定自定义域名 `feigram.dpdns.org`。

页面中的产品图标默认引用 GitHub 仓库中的 FPK 图标资源。
